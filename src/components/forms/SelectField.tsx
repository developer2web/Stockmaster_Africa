import { useState } from 'react'; import { StyleSheet, View } from 'react-native'; import { Button, HelperText, Menu, Text } from 'react-native-paper';
export interface SelectOption { label:string; value:string|null }
export function SelectField({label,value,options,error,required=false,disabled=false,onChange}:{label:string;value:string|null;options:SelectOption[];error?:string;required?:boolean;disabled?:boolean;onChange:(value:string|null)=>void}){const[open,setOpen]=useState(false);const selected=options.find(o=>o.value===value);return <View style={styles.field}><Text variant="labelMedium" style={styles.label}>{label}{required?' *':''}</Text><Menu visible={open} onDismiss={()=>setOpen(false)} contentStyle={styles.menu} anchor={<Button disabled={disabled} style={styles.anchor} mode="outlined" icon="chevron-down" contentStyle={styles.buttonContent} labelStyle={styles.buttonLabel} onPress={()=>setOpen(true)}>{selected?.label??'Sélectionner'}</Button>}>{options.map(o=><Menu.Item key={o.value??'none'} title={o.label} onPress={()=>{onChange(o.value);setOpen(false)}}/>)}</Menu><HelperText type="error" visible={!!error}>{error}</HelperText></View>}
const styles=StyleSheet.create({field:{width:'100%'},label:{marginBottom:6,fontWeight:'700'},anchor:{width:'100%',borderRadius:12},buttonContent:{minHeight:48,justifyContent:'space-between',flexDirection:'row-reverse'},buttonLabel:{flex:1,textAlign:'left'},menu:{maxHeight:360,minWidth:240}});
